name: CI Pipeline 

on:
  push:
    branches:
      - main 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code 
        uses: actions/checkout@v3

      - name: Build Docker Image 
        run: docker build -t static-site .

      - name: Login to Docker Hub
        run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCEKR_PASSWORD }}

      - name: Tag Image
        run: docker tag static-site ${{ secrets.DOCKER_USERNAME }}/static-site:latest
      
      - name: Push Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/static-site:latest 


        
